# 邮件 AI 助手 — 需求文档

> 状态：v1 需求已收敛，数据模型设计中，待补种子数据
> 最后更新：2026-08-14

---

# 整体需求

- 该项目可以接收我提供的一个或若干邮箱账号，然后读取邮箱中的邮件，通过 AI 对**未读**的邮件进行一个总结，从中提炼出重要的信息给我。
- 对于我得到的重要信息，我可以选择性地移动到我的待办之中，DDL 的前一天，项目会提醒我。

## 邮件重要性等级

- **重要**
- **一般**
- **可丢弃**

## 功能1

- 我在**前端**点击**扫描新邮件**后，获取所有**未读**的邮件。
- 帮我分好类，分类通过**树**模式来分类，即一级、二级这样的方式。一级分类通过来源来分类（比如：来自香港理工、来自 JobsDB）。二级分类通过邮件性质来分类（比如：活动、招聘、通知、私人信件……）
- 能够自动帮我剔除掉我不在意的邮件，比如说广告、无关推送。
- 对于筛选完毕的**未读**邮件，先总结，给我一份报告，具体格式如下：
- 「可丢弃」邮件放在报告底部的折叠区。用户展开后，每封邮件都必须显示一段简短的**归类原因摘要**，说明 AI 为什么判断该邮件可丢弃。

```
时间范围：{最早的一封未读邮件的时间 - 最晚的一封未读邮件的时间}
概述：有几封【重要】邮件建议亲自审核，这几封邮件大概讲了什么。

{二级分类名}邮件（以私人邮件为例）：
  1. {人物}提醒你，{时间}要去他的办公室开会。{邮件链接}
  2. ……

{二级分类名}邮件：……（最后需要包含邮件提到的跳转链接）

【可丢弃】邮件（默认折叠）：
  1. {邮件摘要} {邮件链接}
     归类原因：{面向用户的简短原因摘要}
```

## 功能2

- 根据功能1 总结的内容，如果里面出现了类似于「举办时间」「请于 XX 前办理」之类的信息，提取为待办。
- 待办清单需要有一个 `关联邮件` 字段。

## 功能3（一个按钮切换用户模式 / 开发者模式）

- 通过 RL 或其他方式，让 AI 不断了解我的需求，丰富我的人物画像。
- 例如：AI 提炼出的【重要】未读邮件我并不认为重要，我可以人工重新分类，这一行为作为 AI 了解我需求的「数据」。
- 每次更新「数据」，AI 都会重新整合它学到的内容，总结我的需求。

## 功能4（v1.5）

- 报告列表中，每封邮件提供「标记为重要」按钮，将该邮件加入「重要邮件」列表。
- 左侧导航新增「重要邮件」入口，点击进入独立页面，按标记时间倒序展示被标记为重要的邮件，并支持移除标记。
- 「重要邮件」与「本地已读」一致，属于本地标记，不修改邮箱服务器状态。

---

# 技术决策

> D 编号是稳定的决策标识，不代表开发顺序。为便于设计和开发时查阅，以下保留原编号并按主题分组。

## 邮件接入、存储与幂等

| 编号 | 决策 | 结论 |
|---|---|---|
| D1 | 幂等策略 | 本地建 `processed_uid` 表记账；v1 **不自动标已读**。「标为已读」按钮属于 v1.5，届时需要 IMAP 写权限 |
| D3 | 邮件存储与链接 | 正文存本地库。v1 **主链接**指向本站详情页 `/mail/{id}`；v2 再补充回跳原邮箱网页版的**副链接** |
| D9 | 扫描范围 | **时间窗**：`UNSEEN + SINCE {N}天`，默认 N=7，前端可调。不做全量历史清理 |
| D11 | 正文存储 | HTML 转**纯文本**后存 SQLite，**不加密**（单机自用） |
| D12 | 附件 | v1 **只记录文件名与数量**，不下载、不解析 |
| D15 | 邮箱凭据存储 | 账号和 IMAP 非敏感配置存 SQLite；授权码存 `.env`，数据库只记录对应的环境变量名 |
| D18 | 邮件幂等与去重 | 以「邮箱账号 + 文件夹 + `UIDVALIDITY` + UID」作为幂等主键，`Message-ID` 用于辅助识别重复邮件 |
| D25 | 已处理 UID 写入 | 仅当邮件的分类、评分和摘要全部成功后写入已处理记录；若最终档位为「可丢弃」，还必须成功生成 D26 的归类原因摘要。失败邮件保留到下次扫描重试 |

## 来源、分类、评分与可解释性

| 编号 | 决策 | 结论 |
|---|---|---|
| D2 | 一级分类生成 | **规则表优先**：发件人域名/地址 → 来源。未命中时由 AI 建议新来源并写入待确认队列；v1.5 提供确认界面，确认后写入规则表 |
| D5 | 重要性判定 | **打分制**：`score = 规则分 + AI 语义分`，按阈值落三档。权重与阈值均为可调参数，功能3 的反馈直接作用于此 |
| D6 | 二级分类生成 | **固定枚举 + AI 可提议新类**。新类与 D2 复用待确认机制；v1.5 确认后写入分类表 |
| D7 | 【可丢弃】处置 | **仅在报告底部折叠展示**，邮箱完全不动。展开后按 D26 展示每封邮件的归类原因摘要；v1 保持纯只读 |
| D16 | 二级分类存储 | 使用 `category` 表保存，预置初始类别；AI 建议的新类别经确认后可新增，无需修改代码 |
| D19 | 评分明细存储 | 总分、AI 分、AI 理由和最终档位存评分结果表；每条规则命中单独存明细表，便于审计和调整权重 |
| D21 | 待确认建议 | 新来源和新二级分类建议共用一张表，使用建议类型字段区分 |
| D22 | 邮件与分析结果 | 原始邮件与 AI 分析结果分开存储；分类、评分、摘要和归类原因摘要存入可版本化的分析记录，便于重新分析和 v3 反馈扩展 |
| D26 | 【可丢弃】原因摘要 | 每封「可丢弃」邮件都必须生成面向用户的简短原因摘要，说明其低相关性、群发/广告特征、缺少行动要求等主要依据。该摘要由规则命中明细和 AI 理由生成，与分析版本及报告快照一起保存 |

## 扫描、报告与多账号

| 编号 | 决策 | 结论 |
|---|---|---|
| D8 | 报告投递 | v1 只在前端展示，不做推送。功能2 的 DDL 提醒到 v2 时再补推送渠道 |
| D10 | 多账号 | **合并成一份报告**，账号作为邮件的一个属性存储，不拆分报告 |
| D13 | 扫描执行方式 | **同步 + asyncio 并发**（并发度上限 10），不引入任务队列。邮件量上百时会超时，届时再升级为异步任务 |
| D17 | 多邮箱失败策略 | 单个邮箱失败不中断其他邮箱；整次扫描记为「部分成功」，报告中注明失败账号及原因 |
| D20 | 报告存储 | 报告生成后保存快照；快照包含邮件摘要、分类、重要性档位和「可丢弃」归类原因摘要。后续修改分类、规则或阈值时，旧报告不自动变化 |
| D23 | 跨账号重复邮件 | 各邮箱保留独立邮件记录，通过重复组关联；合并报告只展示一份内容，同时注明所属账号 |

## 基础技术与数据生命周期

| 编号 | 决策 | 结论 |
|---|---|---|
| D4 | 技术栈 | Python + FastAPI + SQLite（起步），前端轻量；**LLM 选型见 D14**（本行不再规定厂商） |
| D14 | LLM 接入 | **OpenAI 兼容协议**。`base_url` / `api_key` / `model` 全部走环境变量，代码不绑定任何厂商。首选 **DeepSeek**（`deepseek-chat`） |
| D24 | 基础数据删除 | 邮箱账号、来源规则和分类采用「停用」状态，不物理删除，以保留旧报告的完整关联 |

## 主要决策依赖

| 决策组 | 依赖关系 |
|---|---|
| D1 + D18 + D25 | D18 定义“哪封邮件”的幂等标识，D25 定义所有必需分析产物完成后，何时写入 D1 的已处理记录 |
| D2 + D6 + D16 + D21 | D2/D6 产生来源或分类建议，D21 统一承载建议，D16 保存确认后的二级分类 |
| D5 + D19 + D22 + D26 | D5 给出判定结果，D19 保存判定依据，D22 将结果按分析版本保存，D26 把依据转成用户可读的原因摘要 |
| D7 + D20 + D26 | D7 定义前端展示方式，D26 定义要展示的原因内容，D20 保证旧报告中的内容不随后续规则变动 |
| D10 + D17 + D20 + D23 | D10 定义合并报告，D17 处理部分账号失败，D23 处理跨账号重复邮件，D20 保存最终报告快照 |

## LLM 接入层（D14 细化）

**为什么要抽一层：** DeepSeek 用 OpenAI 兼容协议，Claude 用 Anthropic 协议，两者在**结构化输出**上的做法不同。适配层必须把这个差异吃掉，否则以后换模型要改一堆业务代码。

| 关注点 | DeepSeek（v1 采用） | Claude（日后可切） |
|---|---|---|
| SDK | `openai` Python SDK + 自定义 `base_url` | `anthropic` SDK |
| 结构化输出 | `response_format={"type": "json_object"}` + prompt 里写 schema | tool use / JSON schema |
| 缓存 | **自动上下文缓存**，无需显式声明 | 需显式 `cache_control` |

**DeepSeek 的缓存要点：** 它按前缀自动命中磁盘缓存，命中部分按更低单价计费。所以**系统提示、评分规则、分类枚举这些固定内容必须放在 prompt 最前面且保持字节级稳定**，把变动的邮件正文放最后。顺序写反了缓存就永远打不中。

**分层策略暂不启用：** DeepSeek 单价极低，v1 统一用 `deepseek-chat` 跑打分、分类、摘要。等真的出现质量瓶颈，再按「Haiku 打分 + Sonnet 摘要」的思路分层——接入层已经抽好，届时改配置即可。

配置项（`.env`，不进 git）：

```
LLM_BASE_URL=https://api.deepseek.com
LLM_API_KEY=
LLM_MODEL=deepseek-chat
LLM_CONCURRENCY=10
```

## 评分模型（D5 细化）

```
score = 规则分 + AI 语义分

规则分（确定性，不花 token）：
  发件人在白名单（导师 / 教务处 / 招聘对接人）   +5
  正文含截止日期 / 明确时间点                    +3
  含「请回复」「请确认」「须于」等行动要求        +2
  发件人在广告黑名单                             -5
  含退订链接 / 群发特征                          -3

AI 语义分：0 ~ 5（由模型输出，附一句判断理由）

分档阈值（可调）：
  score >= 8   【重要】
  score 4 ~ 7  【一般】
  score < 4    【可丢弃】
```

- 每封邮件须落库保存 `规则分明细`、`AI 分`、`AI 理由`、`最终档位`，供功能3 复盘与评估集使用。
- 对于最终落入「可丢弃」的邮件，还须根据规则命中明细和 AI 理由生成 `归类原因摘要`，供前端折叠区展示。
- 阈值和权重存配置，不写死在代码里。

## 分类枚举（D6 初始清单，待你增删）

**二级分类（邮件性质）候选：**

| 类别 | 说明 |
|---|---|
| 通知 | 学校/机构的正式通告、政策、截止提醒 |
| 学术 | 课程、选课、成绩、论文、讲座 |
| 活动 | 讲座、社团、招新、社交活动 |
| 招聘 | 职位推送、面试邀约、实习机会 |
| 私人信件 | 具体某个人写给你的信 |
| 账单缴费 | 学费、宿舍费、订阅扣款 |
| 系统与账号 | 密码重置、登录提醒、服务变更 |
| 其他 | 兜底 |

## 邮件接入方式（已验证）

- PolyU Connect（Microsoft 365）**直连 IMAP 不可行**：租户禁用 POP，无用户级 IMAP 开关，OAuth2 需管理员授权。
- 采用**服务器端转发**：PolyU Connect → 个人邮箱（已在 Outlook 网页版验证可对外转发）。
- 程序只处理普通 IMAP + 授权码，不接触微软认证体系。
- ⚠️ 转发须勾选「保留已转发邮件的副本」，避免原件丢失。

---

# 版本规划

| 版本 | 范围 |
|---|---|
| **v1** | 扫描未读 → 规则 + AI 树形分类 → 打分分档 → 生成报告（前端展示，「可丢弃」邮件折叠并显示归类原因摘要）。**纯只读，不写邮箱** |
| v1.5 | 「标为已读」按钮（引入 IMAP 写权限）、待确认队列的确认界面，功能4（重要邮件标记与查看） |
| v2 | 功能2：待办提取 + DDL 前一天提醒；推送渠道；邮件副链接 |
| v3 | 功能3：反馈闭环、人物画像、开发者模式 |

---

# 待确认

**已解决**

- [x] 二级分类是固定枚举还是 AI 自由生成 → D6
- [x] 【重要】的判定依据 → D5
- [x] 【可丢弃】邮件的处置动作 → D7
- [x] 报告与提醒的推送渠道 → D8
- [x] 首次扫描的范围与量级控制 → D9
- [x] 多账号：合并还是分账号 → D10
- [x] 邮件正文本地存储形态 → D11
- [x] 附件是否需要处理 → D12

**待你提供数据**

- [ ] 二级分类初始清单是否需要增删（见上文「分类枚举」表）
- [ ] 一级分类（来源）的初始规则种子：常见发件人域名 → 来源名
- [ ] 白名单种子：导师、教务处、招聘对接人等（可入学后再补）
- [ ] 黑名单种子：常见广告发件人

**待定**

- [x] 扫描任务的执行方式 → D13
- [x] LLM 模型选型与分层策略 → D14

---

# 安全红线（由 D11 引出）

- 邮件正文以明文存于本地 SQLite。**该库文件必须写入 `.gitignore`**。
- `.env`（含 IMAP 授权码、LLM API Key）**必须写入 `.gitignore`**，仓库里只放 `.env.example`。
- 若日后部署到云服务器或将 repo 开源，**必须先补字段加密**。
- 做评估集时用脱敏样本，**真实邮件永不提交进 git**。
